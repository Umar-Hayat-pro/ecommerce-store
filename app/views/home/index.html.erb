    <style>
  /* Hero Section */
  .hero {
    background: url('https://via.placeholder.com/1920x800') no-repeat center center/cover;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    color: white;
    text-align: center;
  }

  .hero-overlay {
    background: rgba(0, 0, 0, 0.4);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-content {
    z-index: 2;
    max-width: 700px;
  }

  .hero h1 {
    font-size: 4rem;
    animation: fade-in 1.2s ease-out;
  }

  .hero p {
    font-size: 1.5rem;
    margin-top: 15px;
    animation: fade-in 1.6s ease-out;
  }

  .hero .btn-primary {
    font-size: 1.2rem;
    padding: 12px 30px;
    margin-top: 20px;
  }

  /* Animation */
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
<div class="hero">
  <div class="hero-overlay"></div>
  <div class="hero-content text-white">
    <h1>Welcome to Ecommerce Store</h1>
    <p>Discover exclusive deals on your favorite products</p>
    <a href="#" class="btn btn-primary btn-lg shadow">Shop Now</a>
  </div>
</div>
